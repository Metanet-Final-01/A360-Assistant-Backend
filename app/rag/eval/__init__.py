"""RAG 검색 평가 하네스 (RPA-131).

골드셋(봇이 실제 쓰는 액션)에 대해 검색 결과의 순위 품질을 MRR·recall@k·hit@k로
측정한다. harness는 search_fn을 주입받는 파라미터 무관 순수 코어라, 어떤 검색 구성
(파라미터·모드)이든 함수로 감싸 넣으면 같은 잣대로 비교할 수 있다 — 하이퍼파라미터
탐색(RPA-132)이 이 위에 올라탄다.
"""
